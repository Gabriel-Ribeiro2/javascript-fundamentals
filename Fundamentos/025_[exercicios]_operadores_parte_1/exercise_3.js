/*
1. Jà temos uma variável chamada Factor
2. Crie um array com 10 valores, esse deve ser a tabuada da variável factor.
3. Exiba o resultado do array no console.
Nota: O resultado deve ser exibido em forma de tabela.
*/

let factor = 5;
let tabuada = [
  factor * 1,
  factor * 2,
  factor * 3,
  factor * 4,
  factor * 5,
  factor * 6,
  factor * 7,
  factor * 8,
  factor * 9,
  factor * 10,
];
console.table(tabuada);
