/*
    Objetos.
    1. crie um objeto chamado client com as seguintes propriedades:
    name: string
    surname: string
    2. Atribua seus dados ao objeto
    3. exiba o nome completo do cliente no console
*/

let client = {
  name: "Gabriel",
  surname: "Ribeiro",
};

console.log(`O nome do cliente é: ${client.name} ${client.surname}`);
