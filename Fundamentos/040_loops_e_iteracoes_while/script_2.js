// E podemos utilizar de forma complementar diferentes
// Tipos de loops, como o while e o for, para resolver problemas diferentes.

let index = 0;

while (index < 10) {
  if (index % 2 === 0) {
    console.log(index);
  }
  index++;
}

// neste caso, o loop
