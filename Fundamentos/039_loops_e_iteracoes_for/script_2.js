/*
Existem muitos detalhes para falar sobre loops
por exemplo, podemos aninhar loops, ou seja, colocar um loop dentro de outro loop.
*/

for (let row = 0; row < 5; row++) {
  for (let column = 0; column < 5; column++) {
    console.log(row, column);
  }
}

/*
Isto é apenas uma introdução ao assunto,
mas existem muitos outros detalhes que iremos ver mais adiante
à medida que for necessário para cada caso
*/
